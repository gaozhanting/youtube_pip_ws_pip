node server.js

安裝插件，打開一個youtube網頁，開啟字幕，使用video PIP

open -n -a "Brave Browser" --args --user-data-dir="/tmp/brave_1"

python3 -m http.server 8081
獨立進程開啟打開(http://localhost:8081/follower.html) -- 這樣PIP的窗口標題短得多，比直接打開文件, 點擊一下出來PIP(流出來自 剛才的視頻頁面的字幕)；實現雙PIP# youtube_pip_ws_pip
